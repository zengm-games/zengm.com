{
  "name": "zengm.com",
  "scripts": {
    "build": "rm -rf _site && yarn run sass && NODE_ENV=production eleventy && yarn run assetgraph && bash tools/after-assetgraph.sh",
    "assetgraph": "rm -rf dist && buildProduction _site/index.html _site/404/index.html _site/.htaccess _site/files/logo-*.png _site/files/carousel/*.png _site/files/*.json --outroot dist --subsetfonts --optimizeimages --browsers \">0.1%, not ie <= 9\" --sharedbundles",
    "start": "rm -rf _site && concurrently --kill-others \"yarn run watch-sass\" \"NODE_ENV=development eleventy --quiet --serve\"",
    "watch-sass": "chokidar \"src/css/*.scss\" -c \"yarn run sass\"",
    "sass": "node-sass --style=compressed -o src/css src/css"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "assetgraph-builder": "^8.1.1",
    "chokidar-cli": "^2.1.0",
    "concurrently": "^6.0.0",
    "husky": "=4",
    "lint-staged": ">=10",
    "node-sass": "^5.0.0",
    "prettier": "^2.2.1",
    "sharp": "^0.27.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,css,md}": "prettier --write"
  },
  "dependencies": {
    "bootstrap-dark-4": "^0.0.9"
  }
}
